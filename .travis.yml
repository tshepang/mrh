language: rust
os:
  - linux
  - osx
  - windows
rust:
  - 1.26.0
  - stable
  - beta
  - nightly
script:
  - cargo build
  - cargo build --features "json"
  - cargo build --features "yaml"
  - cargo build --features "json yaml"
cache: cargo
