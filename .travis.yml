language: rust
os:
  - linux
  - osx
  - windows
rust:
  - 1.33.0
  - stable
  - beta
  - nightly
script:
  - cargo build
  - cargo build --features "json"
  - cargo build --features "yaml"
  - cargo build --features "json yaml"
cache: cargo
